# Architecture Runtimes (Do not check massive pre-compiled Wine binaries into Git history)
# Instead, document how to download them or use a build script to fetch them.
Runtimes/

# Crash logs and diagnostics
*.crash
*.gpu
